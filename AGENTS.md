# CNplus 项目规则（AI 助手读）

## 开工前必读

1. `.hermes/plans/2026-08-21_123014-cnplus-重启-方案B-总纲.md` —— 主纲领
2. 当前任务明确指定的计划、issue 或设计文档
3. 本文件第二节的六条铁律

先运行 `git status --short`，不得覆盖、删除或顺手提交不属于当前任务的改动。

## 六条铁律

1. 自有 AST 节点（不用 Python `ast`，不用裸 dict）
2. 万物带 Span
3. 后端接口先于第二个后端存在
4. 关键字表是数据（`keywords.toml`）
5. 黄金测试语料只增不删
6. 新特性落地时先问「对拍语料覆盖这条路径了吗」，没覆盖先补语料再发版

**与任何任务冲突时，以铁律为准。**

## 语义泄漏防御

转译到 Python 会让 CNplus 悄悄继承 Python 语义。测试必须断言
`docs/spec/00-语义约定.md` **规定的行为**，而不是观察到的宿主行为。
凡「Python 这样所以这样」的地方，标记为未定义行为。

- 不能以“现有测试全绿”证明行为正确；先确认测试覆盖了目标路径。
- 涉及值、求值顺序、错误和容器的改动，必须检查所有适用后端。
- 正常程序要对拍输出和成功退出；错误程序要对拍诊断码，子进程后端还要检查 returncode/stderr。
- 有副作用的表达式必须测试从左到右和只求值一次。
- 后端暂不支持的能力必须显式声明和测试，不得静默跳过。

## 多后端规则

- 语义规范是裁判，任何现有后端和宿主语言都不是裁判。
- 新后端只接 AST 和后端接口，不复制 lexer、parser、checker 或语言语义。
- 共同行为由共享语料验证；后端专有限制写入能力清单，并有明确诊断。
- 对拍测试从后端注册表发现适用后端，不在测试里固定后端数量或名称。
- 新后端落地前，先补齐数字、字符串、容器、相等、求值顺序、错误和 I/O 的边界语料。
- 后端调用宿主 API 前先经过 CNplus 类型检查；宿主异常必须转换为稳定诊断。
- 生成代码的后端必须验证源码映射、退出状态、标准输出和标准错误。

## 每个工作单元的固定流程

1. 明确当前任务范围，写清不处理哪些相邻问题。
2. 读规范、AST、checker、所有适用后端和邻近测试，先定位根因。
3. 写最小复现测试或黄金语料并确认 RED；不接受“推测会失败”。
4. 语义未定义时先补规范或请用户拍板，再写实现。
5. 只修根因；检查同类调用路径，不做任务外清理。
6. 跑聚焦测试、所有适用后端对拍和 `pytest tests/ -q`。
7. 检查 `git diff --check`、`git status --short` 和实际改动文件。
8. 在当前任务的交接处记录根因、测试命令、结果、未解决项和计划书更新位置。

## Git 与并行工作纪律

- 不使用 `git add -A`、`git add .`；只暂存当前工作单元的明确文件。
- 提交前运行 `git diff --cached --name-only`，不得夹带计划、临时文件或其他模型的改动。
- 发现陌生改动先停下核对，不回滚、不覆盖、不假定是自己产生的。
- 不修改 `.hermes/plans/` 中他人正在交接的计划，除非当前任务明确要求更新该计划。
- 不使用脚本批量改源码；现有文件用可审查的逐处 patch。

## 命名与语法原则（面向用户的每个词都要过这一关）

CNplus 是给中文使用者的语言，起关键字、内置函数名、写错误信息时，
**站在零基础用户角度**，不要凭程序员直觉：

1. **先问「中文母语者会怎么说这句话」，不照抄英文语序。**
   反例：`对于 名字 在 成绩`（照抄 `for x in y`）。
   正例：`遍历 成绩 每个 名字`。生硬永远是 bug，
   **不许用「它对应某个英文关键字」为难懂辩护**。

2. **不用纯编程术语。** 术语对老手是路标，对新手是墙。
   反例：`键`（key）。正例：`标签`（贴在值上、靠它找值）。

3. **名字要能让人联想到用途/关系。** 一个词要能牵出它关联的东西。
   反例：`键们`（孤立）。正例：`所有标签`（一眼知道取的是标签）。

4. **不硬造语法。** 别为凑复数生造后缀。
   反例：用「们」表复数（`键们`/`值们`）。正例：`所有…`。

5. **能不写就不写。** 优先让常见操作有最短自然写法。
   例：字典可直接 `遍历 成绩 每个 名字`，不必先 `所有标签(成绩)`。

6. **面向用户的名字优先双字完整词，避免单字歧义。**
   反例：`问`（像虚词）、`除`（除法还是删除？）。
   正例：`询问`、`相除`。另注意「数字」指字形、「数值」指可运算的量。

破坏性改名保持唯一写法，不默认保留旧别名。改名要同步所有适用后端、错误文案、
文档、示例、LSP、语法高亮和测试。

**写法规范（示例/文档必须一致）：**
- 插值字符串（格式串）写作 `@"…{表达式}…"`。**不要再用旧的 `填"…"`**，
  该写法已于 v0.8.0 废除。
- 插值串里嵌套索引用**不转义的单引号**：`@"{人['名字']}"`，
  不要写 `@"{人[\'名字\']}"` —— 双引号插值串里的单引号无需转义。
  已有测试 `test_示例不用多余的引号转义` 守着这条。
- 自定义函数名不要撞内置名（虽然遮蔽是合法的，但对读者是坏示范）。

**改名操作纪律：**
- 不要用脚本批量字符串替换源码；用 patch 逐处修改并审查 diff。
- 确需批量生成的文件，生成后检查重复词根、异常删改和编码，再跑全量测试。
- CHANGELOG 里的历史条目不要跟着改名 —— 那是当时的事实记录。

## 文档与计划

- `AGENTS.md` 只收长期、跨任务规则；当前问题、版本状态和临时决策写进对应计划或 issue。
- 模型规则只在 `AGENTS.md` 维护，不创建其他模型的重复入口文件。
- 代码是当前行为的事实来源；README、CHANGELOG、spec 可能滞后，写文档前必须核对源码并实跑示例。
- 计划写方案、取舍和验收标准，不把尚未拍板的选择写成既定事实。
- 示例必须通过真实 `cnp` 命令运行；各后端能力声明必须分别验证。
- 长中文文件写完后扫描 U+FFFD；发现替换字符先修复编码再交付。

## 约定

- 语言：Python 3.11+，`pytest` 测试
- 开发环境：`.venv`（由 Homebrew 的 `python3.11 -m venv` 建）。
  **本机已弃用 uv**——它管的 Python 被删后 `.venv` 会变死链接。
  重建：`/opt/homebrew/bin/python3.11 -m venv .venv && ./.venv/bin/pip install -e ".[dev,lsp]"`
- 全局命令：`cnp`（`pip3.11 install -e .` 装出来的，写死 3.11 绝对路径，
  不受 shell 里 `python3` 指向哪个版本影响）
- 源文件扩展名：`.cnp`
- 标识符、关键字、错误消息一律中文
- 诊断码稳定：`CN0001` 起，见 `docs/spec/03-诊断码.md`
- parser 恒返回 `(AST, 诊断列表)`，**永不抛异常**（编辑器依赖此性质）

## 发版节奏

**一个工作单元完成且全量测试绿 = 立即发版，不攒版。**
修 bug 发 patch，加特性发 minor。项目早期没有外部用户依赖，
版本号就是改动看板，频繁发版零成本。

发版清单（全部以 cnplus-agent 身份执行）：

1. 全量测试绿（`pytest tests/ -q`）
2. 所有适用后端的正常/错误对拍通过；子进程不得因未检查退出状态而假绿
3. 版本号三处同步：`pyproject.toml`、`cnplus/__init__.py`、
   `editors/vscode/package.json`
4. CHANGELOG「未发布」段定稿为版本号 + 日期 + 一句话标题
5. `vsce package` 重打 vsix（扩展有改动时）
6. 提交、打 tag、`gh release create`、上传 vsix 附件
7. 官网同步：`CNplus-web/src/config/site.ts` 的 version 与 vsix 直链
8. 语言运行时有改动时重建 playground wheel，并断言 wheel METADATA 版本等于网站版本
9. 新增后端时同步其依赖、能力清单、产物和安装文档
10. 官网测试绿后推送，再读取 Release 资产、官网配置和 wheel 版本确认发布完成

## 不要做

- 不要自研 IDE（做 LSP）
- 不要手写 TextMate 语法（从 `keywords.toml` 生成）。
  **改完关键字必须重跑 `python tools/生成语法文件.py`**，否则编辑器里新
  关键字不高亮；`tests/test_语法文件生成.py` 守着这条
- 不要用 `ln -s` 装 VSCode 扩展 —— 新版 VSCode 只加载 `extensions.json`
  登记过的扩展，软链会静默失效（表现为 `.cnp` 显示「纯文本」）。
  用 `vsce package` + `code --install-extension *.vsix`，见
  `docs/贡献/发布vscode扩展.md`
- 不要做增量解析（YAGNI）
- 不要在修 bug 时顺手加入未拍板的新特性
- 不要修改 `archive/`（只读历史）
