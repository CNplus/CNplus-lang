## [未发布]

## [0.9.0] - 2026-08-24 —— 类与对象（方案 C，无继承）

### 语言特性（D-036 定稿语法）
- **类与对象**：`类 学生` + `初始化(名字, 年龄)` + 方法 + `自己`。
  - `初始化` 括号里的参数**自动成为字段**——不写 `自己.x = x` 转存样板；
    块体可省略，要写校验/派生逻辑时才出现
  - `自己` 是隐式关键字：方法签名不写 self 形参，指代「这个方法所属的实例」
  - 实例化与函数调用同形：`学生("小明", 18)`
  - 方法可互调（`自己.等级()`）、可取出传递（取出即绑定实例）
  - 字段可写：`自己.年龄 = 自己.年龄 + 1`，也支持 `+=` 等复合形式
  - 无继承（组合优于继承；将来加 `是一种` 是纯增量）
- `类型(实例)` 返回类名；`打印(实例)` 显示 `<类名>`
- 严格真值、CN0302 未声明检查等既有约定对类同样生效

### 工程同步
- 关键字表加 `类`/`初始化`/`自己`，TextMate 语法重新生成（编辑器高亮同步）
- 新增 `成员赋值语句` AST 节点；类声明、实例化、方法绑定在两后端实现
- 新增 16 个类测试 + 黄金语料 `0018-类与实例`；新增综合示例
  `示例/18-班级成绩册.cnp`（两后端逐字一致）
- 469 个测试全绿

# 更新日志

本项目版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.8.1] - 2026-08-24 —— 报错提示更贴心，编辑器扩展更稳

### 错误提示
- **CN0302（变量未声明）提示重写**：未命中拼写建议时，并列给出两种常见
  意图——「想当文字用但忘了加双引号（`设 名字 = 小明` → `"小明"`）」和
  「想用变量但还没建立」。此前对这类情况给的是「是不是想写小数/小写」
  这类中文编辑距离误猜，纯属误导。
- **废除中文间的模糊拼写猜测**：中文标识符的编辑距离区分度差
  （小明/小写/小数 距离同为 1），数学上无法区分打错字和无关词。
  拼写建议只认两种铁证：名字是候选的超集扩展（`年龄x` ⊃ `年龄`，
  手滑多打了字）、纯 ASCII 名距离 ≤2（`helo` → `hello`）。
- 三个实现点（静态检查、树遍历、转译运行时）同步修改，两后端报错
  输出逐字一致。

### 编辑器扩展
- **不受信任窗口不再静默失败**：工作区不受信任时，扩展此前整体不激活
  （日志零记录，表现为「装了但没反应」，见 #1）。现声明
  `capabilities.untrustedWorkspaces: supported=limited`，受限窗口中
  扩展正常激活并弹提示，附「信任此工作区」按钮；授权后提示一键
  重载窗口启用完整功能。
- VSCode 扩展改为标准 `.vsix` 打包安装。旧的 `ln -s` 软链方式在新版
  VSCode 已失效（只认 `extensions.json` 登记过的扩展），是此前语言模式
  一直显示「纯文本」、高亮不生效的根因。
- 扩展新增**自动探测 Python 解释器**：依次尝试工作区 `.venv`、Homebrew、
  系统 Python，找一个装了 cnplus 的来用，不必手填路径。应对一台机器上
  多个 Python 版本共存的常见情况。
- 新增命令「CNplus: 显示当前使用的解释器」，排查环境问题用。
- 扩展启用官方 Logo 作图标。
- 运行当前文件改用探测到的解释器绝对路径，任意目录的 `.cnp` 都能运行。

### 修复
- **语法文件生成器漏项**：`tools/生成语法文件.py` 的关键字分组是硬编码的，
  v0.4.0 之后新增的 `遍历`/`尝试`/`抛出`/`跳出` 等关键字没进 TextMate
  语法文件，导致编辑器里这些词不高亮。现补全分组，并加自动兜底——
  关键字表里未归组的一律给通用高亮，杜绝再漏（同 D-017 的教训）。
- 新增 `tests/test_语法文件生成.py`：断言 keywords.toml 里每个关键字
  都出现在生成的语法文件中。

### 文档
- 新增 `docs/贡献/发布vscode扩展.md`：`.vsix` 分发流程与已知的坑。

## [0.8.0] - 2026-08-22 —— 插值字符串改用 @ 前缀

### 变更

**破坏性**：格式串（插值字符串）的前缀由关键字 `填` 改为符号 `@`。

| 原写法 | 新写法 |
|---|---|
| `填"你好，{名字}"` | `@"你好，{名字}"` |

`填` 已从关键字表移除，旧写法不再解析。项目处于 0.x 阶段，不保留兼容别名。

改动原因：`填` 是动词，紧跟其后的 `"…"` 在语法上像是它的宾语，
但实际填入的是字符串**内部**的空，动宾关系错位（D-035）。
符号前缀不声称任何动宾关系，歧义消失；同时省去一个关键字，
符合「能不写就不写」原则。

`@` 只在字符串**前缀位置**有特殊含义。字符串内部的 `@` 仍是普通字符，
`打印("联系 a@b.com")` 不受影响。全角 `＠` 归一化为 `@`，两种写法等价。

### 新增
- 4 条回归测试：旧 `填` 已废除、全角 `＠` 等价、字符串内 `@` 不触发插值

### 测试
445 个测试全绿。

## [0.7.2] - 2026-08-22 —— 文档与示例规范

### 修复
- 示例 `17-通讯录` 中格式串内的单引号被多余转义，与既定写法不一致。
  格式串内嵌套索引直接使用单引号即可：`填"{人['名字']}"`

### 新增
- `docs/贡献/release说明规范.md`：发布说明的写作规范。发布说明面向使用者，
  固定「新增 / 变更 / 修复 / 已知限制」结构，语法特性须配可运行示例，
  破坏性变更须给出迁移写法
- 测试 `test_示例不用多余的引号转义`：逐个检查所有示例的引号写法

### 变更
- 全部 17 个已发布版本的说明按新规范重写
- CHANGELOG 各版本标题与发布说明标题统一，改为概括能力而非叙述过程

## [0.7.1] - 2026-08-21 —— 集合元素复合赋值

为了给用户看语言"全貌"，写了 `示例/17-通讯录.cnp`（自然用上目前每一样语法）。
写的过程当场暴露两个真 bug —— 这正是"写真实程序是最好的 debug"的又一次验证。

### 修复
- **索引复合赋值不支持**：`字典[键] += 1`、`列表[i] += 1` 直接解析报错。
  之前只给普通变量 `x += 1` 做了复合赋值，忘了集合元素 ——
  而这是统计类代码的高频写法（分组计数）。
  现在 desugar 成 `字典[键] = 字典[键] + 1`，四种运算符都支持，可嵌套。
- **遮蔽内置名时两后端行为分叉**（更严重）：
  `函数 查找(…)`（`查找` 是内置函数名）在转译后端报 CN0307、树遍历后端放行 ——
  同一段代码两后端结果不同，正是对拍要防的语义泄漏。
  根因：内置名和用户顶层声明装在同一个作用域里，撞了「重复声明」检查。
  修法：**内置名统一放父作用域/父环境**，用户顶层是它的子作用域 ——
  三处（静态检查器、树遍历后端、转译后端）全部对齐。
  现在遮蔽内置名一律允许（`设 查找 = 1` 也行），真重复声明仍报 CN0307。

### 新增
- 示例 `17-通讯录`：全特性巡礼，两后端输出逐字一致
- 14 个回归测试（索引复合赋值 8 个 + 内置名遮蔽一致性 6 个）

### 测试
424 个测试全绿。

## [0.7.0] - 2026-08-21 —— 异常处理

第一座大山：让程序能优雅兜住错误继续跑，而不是一出错就停。

### 新增
- **尝试 / 捕获 / 最后**：
  - `尝试` 放可能出错的代码，`捕获 错` 接住错误（错 里是出错说明）
  - `捕获` 后可不写变量（不关心内容时）
  - `最后` 段**保证执行** —— 哪怕 返回 穿过、或错误继续上抛
- **抛出**：`抛出 "说明"` 主动制造错误，别名 `报错`
- `捕获` 别名 `出错时`，`最后` 别名 `无论如何`
- 新诊断码 `CN0311`（主动抛出未被捕获）
- 示例 `16-安全计算器`，文档 `docs/异常处理.md`

### 变更
- **常见错误的文本在两后端对齐**（按用户要求）：
  - `整数("abc")` / `小数(...)` 转换失败：统一为「没法把 X 变成整数/小数」
  - 内置/库调用出错时，两后端捕获到的 `错` 文本现在逐字一致
- 库异常现在在**调用点**就包成 CNplus错误（CN0303），span 直接取自调用处，
  比之前靠 traceback 行映射推断更可靠

### 测试
407 个测试全绿（新增 17）。异常语义两后端对拍，含「最后必执行」
（正常/被捕获/返回穿过/错误上抛四种路径）的完整覆盖。

## [0.6.1] - 2026-08-21 —— 输入函数更名

### 变更（破坏性）
- `问` → **`询问`**，`问数字` → **`询问数值`**
  - 「问」单字太短、像口语虚词；「询问」是完整动词，一眼看出在向用户要东西
  - 「数字」指字形符号，「数值」指可运算的量 —— 这个函数要的是后者
- 示例里给自己起的函数名 `除` 改为 `相除`
  - 「除」单字有歧义：是「除法」还是「删除」？「相除」只有一个意思
  - 注意：运算符 `/` 的内部名仍是 `除`，那是源码标识符，用户看不到
- 格式串里嵌套引号改用单引号 `填"{t['编号']}"`，不必写 `\"` 转义

旧名一律废除（项目尚无外部依赖，保持唯一写法）。

### 测试
390 个测试全绿。

## [0.6.0] - 2026-08-21 —— 用户输入与格式串

用户指出一个大漏洞：写了 15 个示例，程序全都只能输出、没法接收输入。

### 新增
- **输入**：`问("提示")` 读一段文字，`问数字("提示")` 读数字
  - `问数字` 输错会自动重问，直到输入合法
  - 提示语可省略：`问()`
- **格式化字符串**：`填"你好，{名字}，明年 {年龄 + 1} 岁"`
  - `{}` 里可以放任何表达式（变量、算术、函数调用、索引）
  - `{{` `}}` 表示字面的大括号
- **多返回值 + 解包**：`返回 甲, 乙` 配 `设 甲, 乙 = 函数()`
- 新诊断码 `CN0310`（输入已结束）
- 示例 `07-猜数字` 重写成**真的能玩**的交互游戏
- 示例 `15-待办清单`：完整的交互式软件，把所有语法用了一遍
- 新文档 `docs/输入输出.md`

### 修复
- **`问数字` 在输入耗尽时死循环**（严重）：EOF 后重试会无限循环，
  程序卡死。真实用户按 Ctrl+D 就会触发。现在报 CN0310 并停止。
  两个后端都修，各加了回归测试。
- 转译后端把输入结束误报成「调用 Python 库时出错」，现在正确识别为 CN0310

### 测试
390 个测试全绿（新增 37）：
- 14 个输入测试（含 2 个死循环回归测试）
- 15 个格式串/解包测试
- 11 个综合软件端到端交互测试
- 新增「所有示例双后端输出逐字一致」的整体防护

## [0.5.0] - 2026-08-21 —— 分支与函数补全

对照 Python/C++ 补齐日常书写最影响顺手度的一批语言功能。

### 新增
- **否则如果**：`否则 如果 条件` 多分支链，消灭嵌套地狱
- **默认参数**：`函数 问候(名字, 招呼 = "你好")`，带默认值的参数排在后面
- **关键字参数**：`问候(招呼 = "晚上好", 名字 = "小刚")`，可打乱顺序
- **成员方法调用**：`名单.追加(x)` 等价于 `追加(名单, x)`；
  列表/字典/字符串的内置函数都能点调用
- **自增/自减**：`x++` `x--`（借鉴 C++）
- **复合赋值**：`x += 1` `x -= 1` `x *= 2` `x /= 4`

### 说明
- 自增和复合赋值都走同一套运算函数，语义与普通运算完全一致
  （`x += y` 就是 `x = x + y`，字符串拼接、类型检查一视同仁）
- 成员方法与函数调用等价，是同一个内置函数的两种写法，不是新实现

### 测试
336 个测试全绿（新增 19）。五个特性全部两后端对拍逐字一致。

## [0.4.2] - 2026-08-21 —— 遍历语序调整

用户反馈：`对于 名字 在 成绩` 是照抄英文 `for x in y` 的语序，
既不像自然中文，也没真的帮到未来学 Python 的人 —— 两头不像。

### 变更（破坏性）
- **遍历改用自然中文语序**：`遍历 成绩 每个 名字`（原 `对于 名字 在 成绩`）
  - `遍历` 打头，字面就解释了「逐个走一遍」的含义
  - 集合在前、变量在后，读作「遍历成绩的每个名字」
  - 不保留旧语序别名（项目尚小，保持唯一写法更清晰）
- 关键字 `对于`/`在` 移除，换成 `遍历`/`每个`

### 理由（写入决策 D-021）
语序照抄英文对任何定位都不划算：给小白不够自然，给未来转 Python 的人
也只是凭空造了个「两头不像」的中间物。生硬永远是 bug，不是特性。

### 测试
317 个测试全绿。

## [0.4.1] - 2026-08-21 —— 字典术语调整

用户反馈：字典的「键们」「值们」这套命名生硬看不懂 ——「键」是编程术语，
「们」是硬造的复数后缀。

### 变更（破坏性，但项目尚无外部依赖）
- **「键」概念改叫「标签」** —— 像贴在值上的标签，靠它找到对应的值
  - `键们` → `所有标签`，`值们` → `所有值`
  - `有键` → `有标签`，`删键` → `删标签`
  - 所有错误文案里的「键」改为「标签」
- 强调**字典可直接遍历**：`对于 名字 在 成绩` 就能逐个取标签，
  不必写 `所有标签(成绩)`（示例 12 据此简化）

### 新增
- `docs/列表和字典.md`：用「通讯录」类比讲清标签/值，零基础可读

### 测试
317 个测试全绿。

## [0.4.0] - 2026-08-21 —— 列表、字典与遍历

补上语言最大的结构性缺口：没有集合类型，很多程序根本写不出来。

### 新增
- **列表**：`[1, 2, 3]`，支持索引 `a[0]`、索引赋值 `a[0] = 9`、嵌套
- **字典**：`{"名": "小明", "岁": 18}`，支持 `d["键"]` 读写
- **遍历循环**：`对于 x 在 列表`（别名 `对每个`）
  - 能遍历列表、字典（遍历键）、字符串、范围
  - 遍历变量是块作用域，循环外不可见
- **跳出 / 继续**：提前退出循环、跳过本轮（别名 `中断` / `下一轮`）
- **幂运算** `**`：右结合，优先级高于乘除（`2 ** 3 ** 2 = 512`）
- **30 个内置函数**（原来只有 7 个）：
  - 数学：`绝对值` `最大` `最小` `求和` `四舍五入` `平方根`
  - 列表：`范围` `追加` `插入` `移除` `弹出` `排序` `倒序` `包含` `连接`
  - 字典：`键们` `值们` `有键` `删键`
  - 字符串：`分割` `替换` `查找` `去空白` `大写` `小写` `开头是` `结尾是`
- 新诊断码：`CN0308`（下标越界/缺键）、`CN0309`（不可遍历）
- 示例 `11-名单` `12-成绩统计` `13-猜数字升级`
- 全角方括号【】自动归一化

### 变更
- 静态检查的内置名单改为**从内置表自动读取**，避免加了内置函数忘同步
  （这一版加 30 个函数时就靠它一次全认出来）

### 测试
310 个测试全绿（新增 39）。集合/循环/内置全部两后端对拍，逐字一致。

### 已知限制
- 仍不支持关键字参数 `f(x, 键=值)`
- 中文库包装层（`导入 数学库` 让 API 也中文）留待语言稳定后做

## [0.3.0] - 2026-08-21 —— Python 生态互操作

现在能 `导入` Python 库了，读写文件、上网、算数学都行 ——
网页做不到的桌面/系统功能从此可用。

### 新增
- **Python 转译后端**（`cnplus/backends/python_emit/`）
  - 把 AST 翻译成自包含的 Python 源码再执行
  - 生成的 `.py` **不依赖 CNplus**，可拷到任何 python3 环境独立运行
  - `cnp 运行 --python x.cnp`：转译执行
  - `cnp 编译 x.cnp`：生成独立 `.py` 文件
- **导入语句**：`导入 "math" 作为 数学` / `导入 "random"`（无别名用末段名）
  - 关键字别名：`导入` `引用`
- **成员访问**：`数学.sqrt(16)`，用 `.` 访问库成员
- **内联运行时**（`运行时源码.py`）：强制 CNplus 语义不泄漏到生成代码
  - 每步运算都调运行时，`if 0`、`1=="1"`、`/` 等在转译后端依然守 CNplus 约定
- **行映射**：库异常能精确定位回 `.cnp` 源码行
- 示例 `10-读写文件.cnp`；文档 `docs/借用python库.md`

### 修复（开发中抓到的真 bug）
- 发射器把整块运行时源码当**一行**记映射，导致行号全部错位
  - 修法：`_行` 逐行拆开内嵌换行，各记一条映射
- 块作用域 `_环2`/`_环3` 只起名不发射赋值，运行时 `NameError`
  - 修法：进块时发射 `_环N = 环境(父环)`
- 单参数函数形参 `('n')` 被当字符串而非元组 → 改用列表字面量 `['n']`
- `_定位` 只看最内层帧 → 改为遍历全栈取最深的**非运行时**生成帧

### 测试
269 个测试全绿（新增 61）。核心是**对拍**：
所有黄金语料 + 示例在两个后端跑一遍，输出必须逐字一致 —— 这是防语义泄漏的铁证。

### 已知限制
- 尚不支持关键字参数（`f(x, key=value)`），留待后续版本

## [0.2.0] - 2026-08-21 —— 错误信息与示例

用户反馈：错误提示太晦涩，`阶乘` 这种例子看不懂。这一版专门解决这个。

设计原则：**术语保留，另加一层人话**。抹掉「声明」「布尔值」这些词
反而会让有基础的人重新猜你在说什么 —— 两边都要照顾。

### 新增
- **错误信息三层结构**
  - `消息` —— 保留术语，有基础的人一眼看懂
  - `也就是说` —— 同一件事的大白话版，零基础也能懂
  - `这样改` —— 具体怎么办，经常能直接照抄
- **拼写建议**：名字打错时提示「是不是想写「年龄」？」（编辑距离）
- **关键字白话别名**，效果完全等价：
  - 建立名字 `设` `令` `让`
  - 判断 `如果` `若` `假如`　否则 `否则` `不然` `要不然`
  - 重复 `循环` `当` `重复`　造工具 `函数` `定义` `过程`
  - 给出结果 `返回` `返` `给出`　外层变量 `外部` `全局`
- **9 个生活化示例**，由易到难：你好 → 起名字 → 判断 → 循环 →
  算总价 → 造工具 → 猜数字 → 递归 → 阶乘
  - 原来只有 `阶乘.cnp`，对新手是双重障碍（既要懂数学概念又要懂递归）
  - 现在阶乘排到最后，且加注释解释「5! = 5×4×3×2×1」
- `docs/十分钟上手.md`：零基础教程

### 变更
- 推荐写法从 `令` 改为 `设`（`令` 仍可用）
- 渲染标签：`提示:` → `这样改:`
- 所有错误信息重写，针对具体情形给建议
  - 例：`"字" + 1` 现在提示「把数字转成文字再接起来：… + 文本(1)」

### 测试
207 个测试全绿。新增 45 个：
- 每个示例程序必须能跑（教程里的东西不能是坏的）
- 每条错误必须有大白话解释和具体改法
- **「也就是说」那层不许出现黑话**（标识符、词元、字面量、实参…）
- 白话别名与正式写法结果必须一致

## [0.1.1] - 2026-08-21 —— 语义约定定稿

### 新增
- `docs/spec/00-语义约定.md`：规范定稿（此前只在计划里，现在是正式文档）
  - 八节：值与类型、真值、相等比较、算术、作用域、求值顺序、错误、未定义行为
  - 每条与 Python 的差异都标明**为什么故意不同**
  - 未定义行为单列一节，明确「阶段 4 换 VM 时允许变化」的边界
- `docs/spec/03-诊断码.md`：17 个诊断码及检查时机（静态 / 运行时）
- `tests/test_语义约定.py`：**规范的可执行验证**，31 个测试
  - 测的是规定的行为，不是宿主恰好的行为（防语义泄漏）

### 修复
- **静态检查在短路运算右侧产生假阳性**（由规范测试抓到）
  - `真 或 1 / 0` 被误报 CN0304，但右侧因短路从不执行
  - 修法：静态检查不下钻短路右侧，只查变量名是否声明
  - 原则重申：宁可漏报也不误报

### 变更
- 规范明确：短路跳过的一侧不做类型检查（`真 或 1` 合法，`真 与 1` 报错）
  - 这是短路语义的必然代价，此前未写明

### 测试
162 个测试全绿。

## [0.1.0] - 2026-08-21 —— 编辑器支持

### 新增
- `tools/生成语法文件.py`：从 `keywords.toml` 生成 TextMate 语法（D-007）
  - TextMate 语法技术上是第二套 tokenizer，手写必然与 lexer 漂移
  - 14 条规则，别名与最长匹配自动处理（`不然` 先于 `不`、`//` 先于 `/`）
  - 这是铁律 4「关键字表是数据」的第二次分红
- `cnplus/checker.py`：**静态检查遍**
  - 起因：编辑器不执行代码，`如果 0` 这类求值期错误在编辑器里报不出来
  - 原则：只报能百分百确定的错，拿不准就沉默（假阳性比漏报更烦人）
  - 检查项：字面量条件非布尔、未声明变量、除以字面量零、重复声明
  - 前向引用与递归不误报；变量值静态不可知时沉默
  - 语法有错时跳过静态检查，避免在残缺 AST 上产生噪音
- `cnplus/lsp/server.py`：LSP 服务端（pygls 2.x）
  - **复用同一套 lexer/parser/checker，零重复实现**
  - `didOpen` / `didChange` / `didSave` 实时推送诊断
- `editors/vscode/`：VSCode 扩展（~60 行 JS 的壳）
  - 语法高亮、实时红波浪线、F5 运行、括号自动配对（半角全角都支持）
  - 自动缩进规则（`如果`/`循环`/`函数` 后缩进，`否则` 回退）
  - 本地 `ln -s` 安装即可，无需发布市场

### 变更
- `cnp 检查` 现在包含静态检查，不执行程序就能报出条件类型错误

### 测试
130 个测试全绿（新增 26 个：静态检查真阳性/假阳性防护 + LSP 路径 fuzz）

## [0.0.5] - 2026-08-21 —— 首个可运行版本

**验收通过**：递归阶乘程序跑出 `1 2 6 24 120`；`如果 0` 报出带波浪线的 `CN0301`。

### 新增
- `cnplus/backends/base.py`：`后端` 抽象基类，落实铁律 3
- `cnplus/runtime/values.py`：`函数值`、中文 `类型名`、`显示`（布尔显示为 真/假）
- `cnplus/backends/treewalk/evaluator.py`：树遍历求值器
  - **语义约定在此落实，不继承 Python**：
    - 严格真值 —— 条件必须是布尔，`如果 0` 报 CN0301
    - 跨类型比较报错而非返回假（CN0303）
    - `1 == 1.0` 为真；`/` 恒为小数；除以零报 CN0304
    - 读未声明变量报 CN0302，重复声明报 CN0307
  - 环境链闭包、递归、`与`/`或` 短路
  - 内置函数：`打印` `随机数` `长度` `类型` `整数` `小数` `文本`
- `cnplus/cli.py`：`cnp 运行 / 检查 / 词法 / 语法树 / 版本`
  - `词法` 与 `语法树` 是调试出口，等于把 2022 年的「下标查找器」做正规
- `tests/golden/`：**25 个黄金语料**（15 正常 + 10 错误），落实铁律 5
  - 语料守卫测试防止误删；每条诊断都必须能渲染出波浪线
- `示例/阶乘.cnp`

### 修复
- **归一化把字符串内部的全角标点也换掉了**（由语料 0001 抓到）
  - 根因：lexer 入口对整个源文本归一化，未豁免字符串字面量
  - 修法：字符串内容取原文而非归一化后文本 —— 全角标点在字符串里是数据，不是语法
  - 回归语料：`0015-字符串内全角标点不被归一化`

### 测试
104 个测试全绿。

## [0.0.4] - 2026-08-21 —— 语法分析

### 新增
- `cnplus/parser/ast.py`：**CNplus 自有 AST**，落实铁律 1
  - 22 种节点，全部 `frozen dataclass` + `slots`，全部带 `跨`
  - 不用 Python 的 `ast` 模块，字段结构由 CNplus 语义决定
  - 含 `错误表达式` / `错误语句` 占位节点，供错误恢复填充
- `cnplus/parser/parser.py`：Pratt 解析器 + 恐慌模式错误恢复（D-005）
  - **恒返回 `(程序, 诊断袋)`，永不抛异常** —— 编辑器依赖此性质
  - 7 级优先级：或 < 与 < 相等 < 比较 < 加减 < 乘除 < 调用
  - 一处语句出错不影响后续语句；块内出错不影响块外
  - 内部 `_恢复` 信号绝不逃出 `解析()`；位置不推进时强制吃词元，杜绝死循环
  - 父节点跨度由子节点合并得出
- 77 个测试全绿，含 26 个残缺输入的 fuzz 用例（`令 x =`、`函数 f(`、`@#$%` 等）

## [0.0.3] - 2026-08-21 —— 词法分析

### 新增
- `cnplus/lexer/keywords.toml`：关键字表，落实铁律 4「关键字表是数据」
  - 13 组关键字含别名（`如果`/`若`、`循环`/`当`、`函数`/`定义`）
  - 13 个运算符、4 个标点、18 条全角→半角归一化
- `cnplus/lexer/tokens.py`：`种类` 枚举（41 种）与 `词元`，映射表从 TOML 读取并缓存
- `cnplus/lexer/normalize.py`：全角归一化，**一对一映射保证偏移仍指向原文**
- `cnplus/lexer/lexer.py`：词法器
  - 中文标识符、整数/小数、字符串（含转义）、注释
  - 缩进敏感：产出 `缩进`/`退缩` 词元；空行与纯注释行不影响缩进栈
  - 括号内换行不断句
  - 运算符最长匹配（`<=` 先于 `<`）
  - **遇错报诊断并继续，从不抛异常**（编辑器依赖此性质）
- 30 个测试全绿

### 已知问题
- TOML 规范不允许裸中文作键名，表内所有键已加引号

## [0.0.2] - 2026-08-21 —— 源码位置与诊断

### 新增
- `cnplus/source.py`：`位置` / `跨度` / `源文件`，落实铁律 2「万物带 Span」
  - 跨度支持合并（由子节点算父节点跨度）、单行判定、长度
  - 源文件二分查行、偏移↔行列换算、按跨度取原文
- `cnplus/diagnostics.py`：`诊断` / `诊断袋` / 渲染器
  - 中文错误信息 + 源码摘录 + 波浪线定位 + 可选提示
  - **中文宽字符对齐**：全角字符按两列计算，波浪线不会错位
  - 诊断码分段固定：CN01xx 词法 / CN02xx 语法 / CN03xx 语义运行时
- 包骨架与 uv 环境（Python 3.11），12 个测试全绿

## [0.0.1] - 2026-08-21 —— 项目重启

### 新增
- 项目重启，方案 B 总纲落定
- 仓库骨架：README / CONTRIBUTING / AGENTS / 许可
- 2021–2022 年历史仓库归档至 `archive/`
