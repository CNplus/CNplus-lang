# 参与 CNplus

## 五条铁律（不可协商）

任何改动与之冲突时，以铁律为准；确需违反必须先写 ADR。

1. **自有 AST 节点** —— 不用 Python 的 `ast`，不用裸 dict
2. **万物带 Span** —— 每个 token、每个节点都带起止位置
3. **后端接口先于第二个后端存在**
4. **关键字表是数据** —— `keywords.toml`，不硬编码
5. **黄金测试语料** —— `tests/golden/`，只增不删

## 工作流

- 每个任务 2–5 分钟，TDD 五步：写失败测试 → 跑并确认失败 → 最小实现 → 跑并确认通过 → 提交
- 修 bug 先加语料复现，再修
- 架构变更先在计划文件的决策日志追加一行

## 提交信息

```
<类型>(<范围>): <描述>

类型：feat / fix / docs / test / refactor / chore
范围：lexer / parser / ast / backend / lsp / docs
```

## 版本

从 `v0.0.1` 开始，每完成一组任务发一个小版本，附中文更新日志。
