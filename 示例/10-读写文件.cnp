# 借用 Python 的库，做网页版做不到的事：真正读写电脑上的文件
# 用「导入」把 Python 库拉进来，"库名" 写在引号里，用「作为」起个中文名

导入 "pathlib" 作为 路径库

# 造一个文件路径
设 记事本 = 路径库.Path("/tmp/我的记事本.txt")

# 写入
记事本.write_text("今天学会了用 CNplus 读写文件！", "utf-8")
打印("已经写好了")

# 读回来
设 内容 = 记事本.read_text("utf-8")
打印("文件里写着：" + 内容)
