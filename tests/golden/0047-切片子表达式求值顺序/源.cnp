设 记录 = []

函数 取对象()
    追加(记录, "对象")
    返回 [0, 1, 2, 3, 4, 5]

函数 取起点()
    追加(记录, "起点")
    返回 1

函数 取终点()
    追加(记录, "终点")
    返回 6

函数 取步长()
    追加(记录, "步长")
    返回 2

打印(取对象()[取起点():取终点():取步长()])
打印(记录)
